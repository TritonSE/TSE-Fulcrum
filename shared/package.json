{
  "name": "shared",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/ && rm -f dist/* && tsc",
    "dev": "tsc --watch",
    "prepare": "npm run build"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "typesafer": "github:justinyaodu/typesafer"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
